# homepage是一个导航工具




```shell
docker run --name homepage \
  -e PUID=0 \
  -e PGID=0 \
  -p 3000:3000 \
  -v /overlay/Configs/HomePage:/app/config \
  -v /var/run/docker.sock:/var/run/docker.sock:ro \
  --restart unless-stopped \
  --name HomePage \
  ghcr.io/gethomepage/homepage:latest
```